# csv-consistency-repair
The software discovers structural relationships, reconstructs missing or corrupted values when supported by sufficient evidence, applies minimal reversible edits, preserves ambiguous data unchanged, supports multi-file reconciliation and bounded-memory streaming, and produces machine-readable repair reports with replay and undo verification.
